type Country {
  id: Int!
  name: String!
}

type TeaType {
  id: Int!
  name: String!
}

type Fabric {
  id: Int!
  name: String!
  country: Country!
}

type Query {
  countries: [Country!]!
  fabrics: [Fabric!]!
}
